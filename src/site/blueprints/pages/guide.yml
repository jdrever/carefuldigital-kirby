title: Guide

columns:
  main:
    width: 1/2
    sections:
      fields:
        type: fields
        fields:
          description:
            type: textarea
          image:
            label: Image
            type: files
            query: page.images
          status:
            type: select
            options:
              - Discovery
              - Alpha
              - Beta
              - Live
          mainContent: fields/mainContent
  sidebar:
    width: 1/2
    sections:
      pages:
        label: Sections
        type: pages
        templates:
          - guide-section
          - guide-task-introductions


